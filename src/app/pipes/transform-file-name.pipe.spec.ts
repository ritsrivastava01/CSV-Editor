import { TransformFileNAmePipe } from './transform-file-name.pipe';

describe('TransformFileNAmePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformFileNAmePipe();
    expect(pipe).toBeTruthy();
  });
});
